services:
  - type: web
    name: your-flask-api
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python -m playwright install
    startCommand: gunicorn main:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
